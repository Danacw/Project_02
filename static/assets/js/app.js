// Grab elements from HTML page // Could use D3 for walking though document model
var alertDiv = document.getElementById("alert-row");
var textArea = document.getElementById("user-input");

// Function runs when user presses submit button
function submitUserInformation() {
    //Clean up user's input... not sure if this is needed
    //var userText = textArea.value.replace(/\s+/g, ' ').split(' ').filter((e) => e.length > 0);;

    //Sends a post request to the Flask API
    



}